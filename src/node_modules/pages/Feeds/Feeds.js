import React from "react";
import "./Feeds.scss";
import { makeStyles } from '@material-ui/core/styles';
import Paper from '@material-ui/core/Paper';
import Container from '@material-ui/core/Container';
import Tabs from "pages/Feeds/Tabs/Tabs.js";
import TagsBar from "pages/Feeds/TagsBar/TagsBar.js";

const useStyle = makeStyles({
  paper: {
    flexGrow: "1",
    margin: "0px 20px 0px 0px",
  },
})

const Feeds = (props) => {
  const styles = useStyle();
  const { currentLang } = props;
  return (
    <section className="global-feed">
      <Container style={{ maxWidth: "1124px" }}>
        <div style={{ padding: "100px 0px 0px", display: "flex", alignItems: "flex-start" }}>
          <Paper className={styles.paper}>
            <Tabs currentLang={currentLang} />
          </Paper>
          <TagsBar />
        </div>
      </Container>
    </section>
  )
}

export default Feeds;