{
  "rules": {
    "articles": {
      ".read": "true",
      "$article": {
        ".write": "true",
        "$owner": {
          ".write": "auth != null && auth.uid == $owner"
        }
      }
    },
    "users": {
      "$uid": {
        ".read": "true",
        ".write": "auth != null && auth.uid == $uid"
      }
    }
  }
}
