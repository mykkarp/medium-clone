import React from "react";
import Paper from '@material-ui/core/Paper';
import Container from '@material-ui/core/Container';
import Tabs from "pages/MainPage/Tabs/Tabs.js";
import UserInfo from "pages/UserPage/UserInfo.js";

const UserPage = ({ currentLang, setIsLogin, isLogin }) => {
  return (
    <Container style={{ maxWidth: "1124px" }}>
      <div style={{ padding: "100px 0px 0px" }}>
        <Paper>
          <UserInfo setIsLogin={setIsLogin} isLogin={isLogin} currentLang={currentLang} />
          <Tabs currentPage="userPage" currentLang={currentLang} />
        </Paper>
      </div>
    </Container>
  )
}

export default UserPage;