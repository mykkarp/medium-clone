{
  "rules": {
    "articles": {
      "$article": {
        "$owner": {
          ".read": "true",
          ".write": "auth != null && auth.uid == $owner"
        }
      }
    },
    "users": {
      "$uid": {
        ".read": "true",
        ".write": "auth != null && auth.uid == $uid"
      }
    }
  }
}
